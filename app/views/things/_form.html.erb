<%= form_with(model: thing, local: true) do |f| %>
  <%= f.label :name, '商品名' %>
  <%= f.text_field :name %>
  
  <%= f.label :date, '日付' %>
  <%= f.date_select :date %>

  <%= f.submit '投稿' %>
<% end %>